/*
If the total selling price of 15 items and the total profit earned
on them is input through the keyboard, write a program to
find the cost price of one item.
*/
#include <stdio.h>
void main(){
  int sparr[];
  int profit[];
  for (int i=0 ; i< 5 ; i++){
        printf("Enter the selling price");
        scanf("%d",sparr[i]);
        printf("Enter the profit of the above product");
        scanf("%d",profit[i]);
    }
  for (int i=0; i<5 ; i++){
        int temp =
        printf("Total cost price of the above product is %d",);
    }

}
